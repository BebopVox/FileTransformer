#pragma once

#define B(x) (x)
#define KB(x) (x * 1024)
#define MB(x) (KB(x) * 1024)
#define GB(x) (MB(x) * 1024)

#define PART_EXT              ".ft"
#define PART_INFO_SEPARATOR   "..."
#define PART_SIZE             MB(1)
